# Run once manually after cluster bootstrap:
#   kubectl create namespace argocd
#   kubectl apply -f clusters/homelab/bootstrap/argocd-install.yml
#
# Then apply the app-of-apps:
#   kubectl apply -f clusters/homelab/bootstrap/app-of-apps.yml
apiVersion: v1
kind: Namespace
metadata:
  name: argocd
