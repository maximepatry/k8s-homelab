extends: default

rules:
  line-length:
    max: 150
    level: warning
  comments:
    min-spaces-from-content: 1
  truthy:
    # allow yes/no used in Ansible tasks
    allowed-values: ["true", "false", "yes", "no"]
    check-keys: false
  braces:
    min-spaces-inside: 0
    max-spaces-inside: 1
