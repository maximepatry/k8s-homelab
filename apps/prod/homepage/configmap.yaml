apiVersion: v1
kind: ConfigMap
metadata:
  name: homepage
  namespace: prod
  labels:
    app.kubernetes.io/name: homepage
data:
  kubernetes.yaml: |
    mode: cluster
  settings.yaml: |
    title: Homelab
    background: https://images.unsplash.com/photo-1451187580459-43490279c0fa
    theme: dark
  custom.css: ""
  custom.js: ""
  bookmarks.yaml: |
    - Repo:
        - k8s-homelab:
            - abbr: GH
              href: https://github.com/maximepatry/k8s-homelab
  services.yaml: |
    - Homelab:
        - Grafana:
            href: http://grafana.homelab.local
            description: Metrics dashboards (Prometheus + Grafana)
            icon: grafana.png
  widgets.yaml: |
    - kubernetes:
        cluster:
          show: true
          cpu: true
          memory: true
          showLabel: true
          label: "cluster"
        nodes:
          show: true
          cpu: true
          memory: true
          showLabel: true
    - resources:
        backend: resources
        expanded: true
        cpu: true
        memory: true
        network: default
    - search:
        provider: duckduckgo
        target: _blank
  docker.yaml: ""
  # v2.3.0 added proxmox.yaml to its required skeleton config set (checked
  # on every startup) - without it present, the container tries to copy its
  # own skeleton file into /app/config, which fails EACCES because that dir
  # is a read-only ConfigMap mount, and it CrashLoopBackOffs. Not used here
  # (no Proxmox in this homelab, see bare-metal/README.md "History") - just
  # needs to exist.
  proxmox.yaml: ""
